# Related repositories

## bext — ShellDeck server APIs

- Local repository: `/home/karim/Desktop/programming-laboratory/wd29/bext`
- GitHub remote: `git@github.com:benfavre/bext.git`
- The shared Manage server routes consumed by ShellDeck live in:
  `sites/shared/manage/src/routes/`
- The main ShellDeck route files are `shelldeck-auth.ts`,
  `shelldeck-sync.ts`, `shelldeck-sites.ts`, `shelldeck-support.ts`,
  `shelldeck-issues.ts`, and `shelldeck-fleet.ts`.
- The Manage application that embeds these routes lives in:
  `sites/inklura-manage-prism/`

When work spans the ShellDeck client and its server API, inspect the local
`bext` repository above instead of searching for or cloning another copy.
